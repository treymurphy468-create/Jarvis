export const LOOPBACK_HOST = '127.0.0.1';
export const SERVER_PORT = 3847;
export const VITE_PORT = 5173;

export const SERVER = `http://${LOOPBACK_HOST}:${SERVER_PORT}`;
export const WS_URL = `ws://${LOOPBACK_HOST}:${SERVER_PORT}/ws/events`;
export const VITE_URL = `http://${LOOPBACK_HOST}:${VITE_PORT}`;
